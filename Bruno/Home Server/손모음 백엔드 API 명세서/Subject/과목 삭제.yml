info:
  name: 과목 삭제
  type: http
  seq: 4
  tags:
    - Subject

http:
  method: DELETE
  url: "{{baseUrl}}/api/v1/subjects/:subjectId"
  params:
    - name: subjectId
      value: ""
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: OK
    request:
      url: "{{baseUrl}}/api/v1/subjects/:subjectId"
      method: DELETE
      params:
        - name: subjectId
          value: ""
          type: path
    response:
      status: 200
      statusText: OK
      body:
        type: text
        data: ""
