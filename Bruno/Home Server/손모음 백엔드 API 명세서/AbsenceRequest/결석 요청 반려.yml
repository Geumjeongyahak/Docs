info:
  name: 결석 요청 반려
  type: http
  seq: 3
  tags:
    - AbsenceRequest

http:
  method: PATCH
  url: "{{baseUrl}}/api/v1/absence-requests/:requestId/reject"
  params:
    - name: requestId
      value: ""
      type: path
  body:
    type: json
    data: |-
      {
        "note": "사유가 충분하지 않습니다."
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: OK
    request:
      url: "{{baseUrl}}/api/v1/absence-requests/:requestId/reject"
      method: PATCH
      params:
        - name: requestId
          value: ""
          type: path
      body:
        type: json
        data: |-
          {
            "note": "사유가 충분하지 않습니다."
          }
    response:
      status: 200
      statusText: OK
      headers:
        - name: Content-Type
          value: "*/*"
      body:
        type: text
        data: |-
          {
            "id": 1,
            "lessonId": 1,
            "lessonDate": "2026-03-28",
            "requestedById": 3,
            "requestedByName": "홍길동",
            "reason": "",
            "status": "PENDING",
            "approvalAt": "",
            "approvalByName": "",
            "note": "",
            "createdAt": ""
          }
