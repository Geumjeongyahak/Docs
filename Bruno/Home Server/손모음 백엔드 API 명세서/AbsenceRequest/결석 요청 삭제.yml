info:
  name: 결석 요청 삭제
  type: http
  seq: 6
  tags:
    - AbsenceRequest

http:
  method: DELETE
  url: "{{baseUrl}}/api/v1/absence-requests/:requestId"
  params:
    - name: requestId
      value: ""
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: OK
    request:
      url: "{{baseUrl}}/api/v1/absence-requests/:requestId"
      method: DELETE
      params:
        - name: requestId
          value: ""
          type: path
    response:
      status: 200
      statusText: OK
      body:
        type: text
        data: ""
