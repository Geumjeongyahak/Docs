info:
  name: 사용자 부서 참여
  type: http
  seq: 2
  tags:
    - User Department

http:
  method: POST
  url: "{{baseUrl}}/api/v1/users/:userId/departments"
  params:
    - name: userId
      value: ""
      type: path
      description: 사용자 ID
  body:
    type: json
    data: |-
      {
        "departmentId": 1
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: OK
    request:
      url: "{{baseUrl}}/api/v1/users/:userId/departments"
      method: POST
      params:
        - name: userId
          value: ""
          type: path
          description: 사용자 ID
      body:
        type: json
        data: |-
          {
            "departmentId": 1
          }
    response:
      status: 200
      statusText: OK
      body:
        type: text
        data: ""
