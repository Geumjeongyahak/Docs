info:
  name: 첨부파일 삭제
  type: http
  seq: 6
  tags:
    - File

http:
  method: DELETE
  url: "{{baseUrl}}/api/v1/files/attachments/:fileId"
  params:
    - name: fileId
      value: ""
      type: path
      description: 삭제할 파일 ID. files 테이블의 UUID 기본 키입니다.
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: OK
    request:
      url: "{{baseUrl}}/api/v1/files/attachments/:fileId"
      method: DELETE
      params:
        - name: fileId
          value: ""
          type: path
          description: 삭제할 파일 ID. files 테이블의 UUID 기본 키입니다.
    response:
      status: 200
      statusText: OK
      body:
        type: text
        data: ""
