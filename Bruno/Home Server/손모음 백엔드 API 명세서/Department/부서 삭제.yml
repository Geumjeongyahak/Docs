info:
  name: 부서 삭제
  type: http
  seq: 3
  tags:
    - Department

http:
  method: DELETE
  url: "{{baseUrl}}/api/v1/departments/:id"
  params:
    - name: id
      value: ""
      type: path
      description: 부서 ID
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: OK
    request:
      url: "{{baseUrl}}/api/v1/departments/:id"
      method: DELETE
      params:
        - name: id
          value: ""
          type: path
          description: 부서 ID
    response:
      status: 200
      statusText: OK
      body:
        type: text
        data: ""
