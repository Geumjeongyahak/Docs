info:
  name: 사용자 상세 조회
  type: http
  seq: 3
  tags:
    - User

http:
  method: GET
  url: "{{baseUrl}}/api/v1/users/:userId"
  params:
    - name: userId
      value: ""
      type: path
      description: 사용자 ID
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: OK
    request:
      url: "{{baseUrl}}/api/v1/users/:userId"
      method: GET
      params:
        - name: userId
          value: ""
          type: path
          description: 사용자 ID
    response:
      status: 200
      statusText: OK
      headers:
        - name: Content-Type
          value: "*/*"
      body:
        type: text
        data: |-
          {
            "id": 1,
            "username": "user1324",
            "name": "홍길동",
            "email": "user@example.com",
            "phoneNumber": "010-1234-5678",
            "roles": "ADMIN"
          }
