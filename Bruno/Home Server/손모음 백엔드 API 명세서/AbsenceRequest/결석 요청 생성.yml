info:
  name: 결석 요청 생성
  type: http
  seq: 2
  tags:
    - AbsenceRequest

http:
  method: POST
  url: "{{baseUrl}}/api/v1/absence-requests"
  body:
    type: json
    data: |-
      {
        "lessonId": 1,
        "reason": "개인 사정으로 인한 결석"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: OK
    request:
      url: "{{baseUrl}}/api/v1/absence-requests"
      method: POST
      body:
        type: json
        data: |-
          {
            "lessonId": 1,
            "reason": "개인 사정으로 인한 결석"
          }
    response:
      status: 200
      statusText: OK
      headers:
        - name: Content-Type
          value: "*/*"
      body:
        type: text
        data: |-
          {
            "id": 1,
            "lessonId": 1,
            "lessonDate": "2026-03-28",
            "requestedById": 3,
            "requestedByName": "홍길동",
            "reason": "",
            "status": "PENDING",
            "approvalAt": "",
            "approvalByName": "",
            "note": "",
            "createdAt": ""
          }
