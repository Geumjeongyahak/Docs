info:
  name: 사용자 삭제
  type: http
  seq: 4
  tags:
    - User

http:
  method: DELETE
  url: "{{baseUrl}}/api/v1/users/:userId"
  params:
    - name: userId
      value: ""
      type: path
      description: 사용자 ID
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: OK
    request:
      url: "{{baseUrl}}/api/v1/users/:userId"
      method: DELETE
      params:
        - name: userId
          value: ""
          type: path
          description: 사용자 ID
    response:
      status: 200
      statusText: OK
      body:
        type: text
        data: ""
