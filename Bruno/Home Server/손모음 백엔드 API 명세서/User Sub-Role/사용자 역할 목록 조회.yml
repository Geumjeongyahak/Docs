info:
  name: 사용자 역할 목록 조회
  type: http
  seq: 1
  tags:
    - User Sub-Role

http:
  method: GET
  url: "{{baseUrl}}/api/v1/users/:userId/roles"
  params:
    - name: userId
      value: ""
      type: path
      description: 사용자 ID
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: OK
    request:
      url: "{{baseUrl}}/api/v1/users/:userId/roles"
      method: GET
      params:
        - name: userId
          value: ""
          type: path
          description: 사용자 ID
    response:
      status: 200
      statusText: OK
      headers:
        - name: Content-Type
          value: "*/*"
      body:
        type: text
        data: |-
          [
            {
              "name": "ADMIN",
              "level": 100,
              "code": 1
            }
          ]
