info:
  name: 교실 상세 조회
  type: http
  seq: 1
  tags:
    - Classroom

http:
  method: GET
  url: "{{baseUrl}}/api/v1/classrooms/:id"
  params:
    - name: id
      value: ""
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: OK
    request:
      url: "{{baseUrl}}/api/v1/classrooms/:id"
      method: GET
      params:
        - name: id
          value: ""
          type: path
    response:
      status: 200
      statusText: OK
      headers:
        - name: Content-Type
          value: "*/*"
      body:
        type: text
        data: |-
          {
            "id": 1,
            "name": "해바라기 반",
            "type": "WEEKDAY",
            "description": "이 교실은 수학 수업을 위한 교실입니다.",
            "createdAt": "2024-01-01T12:00:00",
            "updatedAt": "2024-01-02T12:00:00"
          }
