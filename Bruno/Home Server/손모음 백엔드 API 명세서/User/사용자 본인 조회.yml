info:
  name: 사용자 본인 조회
  type: http
  seq: 6
  tags:
    - User

http:
  method: GET
  url: "{{baseUrl}}/api/v1/users/me"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: OK
    request:
      url: "{{baseUrl}}/api/v1/users/me"
      method: GET
    response:
      status: 200
      statusText: OK
      headers:
        - name: Content-Type
          value: "*/*"
      body:
        type: text
        data: |-
          {
            "id": 1,
            "username": "user1324",
            "name": "홍길동",
            "email": "user@example.com",
            "phoneNumber": "010-1234-5678",
            "roles": "ADMIN"
          }
