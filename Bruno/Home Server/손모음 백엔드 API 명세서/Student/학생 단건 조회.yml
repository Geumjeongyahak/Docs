info:
  name: 학생 단건 조회
  type: http
  seq: 3
  tags:
    - Student

http:
  method: GET
  url: "{{baseUrl}}/api/v1/students/:studentId"
  params:
    - name: studentId
      value: ""
      type: path
      description: 학생 식별자
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: OK
    request:
      url: "{{baseUrl}}/api/v1/students/:studentId"
      method: GET
      params:
        - name: studentId
          value: ""
          type: path
          description: 학생 식별자
    response:
      status: 200
      statusText: OK
      headers:
        - name: Content-Type
          value: "*/*"
      body:
        type: text
        data: |-
          {
            "id": 1,
            "name": "홍길동",
            "phoneNumber": "010-1234-5678",
            "description": "학생입니다.",
            "status": "ENROLLED"
          }
