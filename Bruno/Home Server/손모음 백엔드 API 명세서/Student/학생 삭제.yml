info:
  name: 학생 삭제
  type: http
  seq: 4
  tags:
    - Student

http:
  method: DELETE
  url: "{{baseUrl}}/api/v1/students/:studentId"
  params:
    - name: studentId
      value: ""
      type: path
      description: 학생 식별자
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: OK
    request:
      url: "{{baseUrl}}/api/v1/students/:studentId"
      method: DELETE
      params:
        - name: studentId
          value: ""
          type: path
          description: 학생 식별자
    response:
      status: 200
      statusText: OK
      body:
        type: text
        data: ""
